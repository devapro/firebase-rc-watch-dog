{
  "name": "config",
  "scripts": {
    "compile": "cp ../tsconfig.json.template ./tsconfig.json && tsc"
  },
  "dependencies": {
    "cron": "^2.1.0",
    "firebase-admin": "^9.12.0",
    "google-auth-library": "^6.0.3",
    "googleapis": "^52.1.0",
    "request": "^2.88.2"
  }
}
